project_id = "ubnlvxkjemzhvmulowhj"

# Webhook público — recebe mensagens do servidor WhatsApp QR.
# Protegido por WHATSAPP_QR_TOKEN no header x-qr-token.
[functions.whatsapp-qr-incoming]
verify_jwt = false

# Envio de mensagens — exige usuário autenticado da plataforma.
[functions.send-whatsapp-message]
verify_jwt = true
